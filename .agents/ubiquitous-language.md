# Ubiquitous Language

> Single source of truth for domain terminology. Update when entities, relationships,
> or naming conventions change. AI tools should read this before inspecting source files.
>
> **Last updated**: 2026-07-10 (Fix Codex backend — `CodexItem`'s field renamed `item_type`→`type` and `turn.failed` now reads `error.message` to match a captured real stream from Codex CLI 0.143.0; `agent_spawn`/`agentProcessService.spawn` gained `keepStdinOpen` so a one-shot process like `codex exec` closes stdin instead of hanging forever waiting for input; Chat thinking UX — `agent_reasoning` split off `AgentEvent`/`ChatItem` as its own kind, rendered by the new `ReasoningBlock`; `ThinkingIndicator` added as an in-flight loader for the duration of `turnActive`; `build_tree` hides `AGENT_CONTEXT_FILES` (`AGENTS.md`/`CLAUDE.md`) from the sidebar tree at the vault root — they remain directly readable/writable, just not sidebar-navigable; Settings model selector — `Settings.model` (an `AgentModel`) replaces `Settings.engine`; the engine is derived from the model; `AgentBackend.start` passes `--model`; Chat engine selector removed — engine chosen only in SettingsDialog; Vault agent context: `AGENTS.md` + `CLAUDE.md` scaffolded into every adopted vault root; Sidebar create affordance: `EntryDraft` + `EntryDraftRow` + `createVaultEntry`, reached from the vault header's two buttons or the `a`/`A` sidebar chords; Chat chrome: `AiFab` moved to the top-right; `ChatPanel`'s header bar stripped to a bare engine selector; Hideable sidebar: `sidebarOpen` gates the sidebar `FocusRegion`, toggled by `Ctrl-w b` or the StatusBar button; Vault entry creation backend: guarded `create_note`/`create_directory` commands; `read_vault_tree` now surfaces empty directories)
> **Canonical types**: `src/` (TS), `src-tauri/src/` (Rust)

---

## How to maintain this document

1. **Add terms** when a new entity, state, or process enters the codebase.
2. **Add aliases to avoid** when ambiguity appears in PRs, chats, or AI-generated code.
3. **Update relationships** when entity connections change (new FK, removed link, etc.).
4. **Flag ambiguities** when a term means different things in different contexts.
5. **Bump "Last updated"** on every edit so AI tools know how fresh the context is.

---

## Core entities

| Term | Canonical type | Aliases to avoid | Notes |
|------|-----------------|-------------------|-------|
| Focus region | `FocusRegion` (`src/stores/app-store.ts`) | "pane", "panel" (as a state value) | One of `'sidebar' \| 'viewer' \| 'chat'`. The app-level focus target — not to be confused with the editor's own DOM focus. |
| Global mode | `GlobalMode` (`src/stores/app-store.ts`) | "vim mode" (ambiguous — see Flagged ambiguities) | One of `'normal' \| 'command'`. The **app-level** vim mode driven by the global keymap, distinct from CodeMirror's own vim mode (`@replit/codemirror-vim`), which is a separate system owned by the editor slice. |
| App store | `useAppStore` (`src/stores/app-store.ts`) | "global store", "app context" | Zustand store. Single source of truth for `activeRegion`, `mode`, `sidebarOpen`, `chatOpen`, `vaultRoot`. `vaultRoot` lives here (not a filesystem-specific store) so slices that need the vault path (e.g. the agent's `cwd`) don't depend on the sidebar/filesystem slice. |
| Sidebar visibility | `sidebarOpen` (`src/stores/app-store.ts`) | "sidebar collapsed" (that's `FileTreeItem.collapsed`, a *directory row* — see Flagged ambiguities) | Whether the sidebar region is shown (#38). Gates a `FocusRegion`, so `reachableRegions` filters `'sidebar'` out when hidden and `closeSidebar` reassigns `activeRegion` to `'viewer'` — exactly as `chatOpen` does for the chat. Per-session view state, deliberately **not** persisted in `Settings`. Toggled by `Ctrl-w b` (both keymaps) or the StatusBar `sidebar` button. |
| Global keymap | `useGlobalKeymap` (`src/hooks/use-global-keymap.ts`) | "keybindings", "shortcuts" | The app-level `Ctrl-w` chord (`h`/`l` region jump, `b` sidebar toggle, `c` chat toggle, `s` settings-dialog toggle) + `:`/`Escape` mode transitions. Registered on `window` in the bubble phase so a future CodeMirror instance can `stopPropagation()` to keep its own top-precedence handling authoritative. |
| AiFab | `AiFab` (`src/components/ai/AiFab.tsx`) | "chat button", "toggle button" | The floating action button that opens/closes the chat region. The **only** place the reserved lime accent (`--fab-accent`) is rendered. Anchored **top-right** of the content area (#39): it floats over `ChatPanel`'s header-less top row when open, and clears `Viewer`'s bottom-right vim-mode badge when closed. Carries no `z-index` — it paints above `ChatPanel` and below `SettingsDialog`'s scrim purely by DOM order in `Layout`. |
| Editor store | `useEditorStore` (`src/stores/editor-store.ts`) | "document store", "file store" | Zustand store owned by the editor slice. In-memory only: `content`, `dirty`, `vimMode`, `saveIntent`, `filePath`. No filesystem I/O in this slice — the sidebar open/save loop (#14) drives `openFile`/`markSaved` against `vaultService`. `filePath` is `null` for the unsaved scratch buffer shown before any file is opened. |
| Editor vim mode | `EditorVimMode` (`src/stores/editor-store.ts`) | "vim mode" (ambiguous — see Flagged ambiguities) | One of `'normal' \| 'insert' \| 'visual' \| 'replace'`. CodeMirror's own vim mode (`@replit/codemirror-vim`), synced into `useEditorStore` by `vimModeSync` (`src/lib/editor/vim-mode-sync.ts`) via the `vim-mode-change` event. Distinct from the app-level `GlobalMode`. |
| Save intent | `saveIntent` (`src/stores/editor-store.ts`) | "save event", "save signal" | A monotonically increasing counter on `useEditorStore`, bumped by `requestSave()` on `:w` (vim ex-command, `src/lib/editor/save.ts`) or `Mod-s`. Represents "a save was requested" without performing any I/O — later slices subscribe to it to trigger the actual disk write. |
| Region exit (editor) | `regionExit` (`src/lib/editor/region-exit.ts`) | "vim keymap", "ctrl-w handler" | CM6 extension implementing the same `Ctrl-w` + `h`/`l`/`b`/`c`/`s` chord as `useGlobalKeymap`, at `Prec.highest` so the editor is authoritative while focused (doc/v0-spec.md §3.4 "CodeMirror coexistence rule"). Explicitly stops DOM propagation so `useGlobalKeymap`'s window-level listener never double-handles the same keydown. |
| Wikilink (editor) | `wikilink` (`src/lib/editor/wikilink.ts`) | "link decoration" | Inline `[[wikilink]]` rendering inside the CM6 markdown source (underline + hover, no color, per the design system's `Wikilink` component). A hand-built CM6 `ViewPlugin` decoration, not a mounted React component. |
| VaultEntry | `VaultEntry` (`src-tauri/src/vault.rs`), mirrored as a TS interface in `src/services/vault.service.ts` | "file node", "tree node" | A file or directory inside the open vault. Files are filtered to `.md` notes, but **every** directory is included — an empty one too (#36), so a freshly created folder appears in the sidebar before it holds any note. Hidden entries (`.git`, `.obsidian`, …) are always skipped. |
| Vault entry creation | `create_note` / `create_directory` (`src-tauri/src/vault.rs`), exposed as `vaultService.createNote` / `vaultService.createDirectory` | "new file command", "touch" | Guarded creation (#36) of an empty note — `.md` appended when the name lacks it, since `build_tree` only surfaces Markdown — or of an empty directory, inside the open vault root. Both resolve through the same `guarded_path` root check as `read_note`/`write_note`, require an already-existing parent directory (nested creation is never implicit), and are atomic: a name collision fails with `AlreadyExists` rather than clobbering. Each returns the created path so the caller can open or reveal it. Their only frontend consumer is `createVaultEntry` (#40). |
| Vault agent context | `AGENT_CONTEXT_FILES` / `scaffold_agent_context` (`src-tauri/src/vault.rs`), templates in `src-tauri/templates/` | "vault README", "system prompt", "project instructions" (that's the engines' term for the file, not ours) | The `AGENTS.md` + `CLAUDE.md` pair scaffolded into a vault root whenever it is adopted (#41), so the headless engines — which run with `cwd` = the vault — load instructions describing the vault (doc/v0-spec.md §4.4). `CLAUDE.md` is a thin `@AGENTS.md` import, mirroring this repo's own bridge pattern, so Codex (which reads `AGENTS.md`) and Claude Code see one source of truth. Written per file with `create_new`, so a hand-edited or foreign file is never clobbered; deleting one restores just that one on the next open. Both are plain `.md` and remain directly readable/writable, but `build_tree` hides them from the sidebar at the vault root (#50) — they're for the agent to read from `cwd`, not for the user to browse. |
| VaultState | `VaultState` (`src-tauri/src/vault.rs`) | — | Rust-side `tauri::State`: the currently open vault root path plus the live `notify::RecommendedWatcher` watching it. Replacing the watcher (via `pick_vault`) drops and thus stops the previous one. |
| vaultService | `vaultService` (`src/services/vault.service.ts`) | "fs service", "file service" | Typed wrapper over the Rust vault commands (`pick_vault`, `open_vault`, `read_vault_tree`, `read_note`, `write_note`, `create_note`, `create_directory`) and the `vault-changed` event. Deliberately does not import `useAppStore` — callers own writing the picked path into `vaultRoot` — so the filesystem data layer stays decoupled from app-level state. |
| `vault-changed` (event) | emitted by `src-tauri/src/vault.rs`, consumed via `vaultService.onVaultChanged` | — | Fired by the Rust `notify` watcher on every filesystem event inside the open vault root. Payload is the list of touched absolute paths (as strings). |
| Agent event | `AgentEvent` (`src/lib/agent/agent-event.ts`) | "message", "chunk" | Discriminated union (`type` field) of the seven v0 event kinds: `agent_message`, `agent_reasoning`, `tool_call_start`, `tool_call_result`, `permission_request` (unused in v0), `turn_done`, `error`. The chat panel consumes **only** this union, never an engine's raw JSON. `agent_reasoning` (#49) split off from `agent_message` — each backend now emits reasoning/thinking blocks as their own event instead of concatenating them into the final text. |
| Agent source | `AgentSource` (`src/lib/agent/agent-event.ts`) | "engine", "provider" (as this type's name) | One of `'claude-code' \| 'codex'`. Carried on every `AgentEvent` for logs/debug — **never** used to branch UI. |
| Agent backend | `AgentBackend` (`src/lib/agent/agent-backend.ts`) | "adapter" (that's the per-engine translator implementing this interface, not the interface itself — see Flagged ambiguities) | The interface every engine (v0: `external-CLI` Claude Code/Codex; later: `native-provider`) implements: `start(cwd, model, onEvent)`/`send`/`stop`, emitting `AgentEvent`s via an `onEvent` callback. The `model` string is passed through to the CLI's `--model` flag. |
| Backend registry | `registerBackend` / `getBackend` / `listBackends` (`src/lib/agent/backend-registry.ts`) | "backend store" | Runtime `Map<AgentSource, AgentBackend>`. Each backend module registers itself; the chat store looks up the backend derived from the selected model. Populated at boot by `registerBuiltinBackends()` (`src/lib/agent/backends/index.ts`), called once from `main.tsx`. |
| AgentModel | `AgentModel` / `AgentModelId` (`src/lib/agent/model-catalog.ts`) | "model id" (use the type `AgentModelId` when you mean the id union) | A user-selectable LLM. `id` is persisted in `Settings.model`; `label` is shown in the Settings model picker; `engine` is the `AgentSource` (CLI) that runs it — **derived from the model, never persisted separately**; `cliModel` is the exact string passed to that CLI's `--model` flag. The four v0 models: GPT 5.4 mini / GPT 5.5 fast (`codex`), Haiku 4.5 / Sonnet 5 (`claude-code`). |
| Model catalog | `AGENT_MODELS` / `listModels` / `getModel` / `DEFAULT_MODEL_ID` (`src/lib/agent/model-catalog.ts`) | "model list" | The static list the Settings picker renders and the chat/boot seed resolve against. `DEFAULT_MODEL_ID` (`gpt-5.4-mini`) mirrors Rust `default_model()`. |
| Sidebar store | `useSidebarStore` (`src/stores/sidebar-store.ts`) | "tree store", "vault store" | Zustand store owned by the sidebar slice: the last-read `tree` (`VaultEntry[]`), the `expanded` directory-path set, `cursorPath` (the vim keyboard cursor row), and the pending `draft` (#40). Deliberately separate from `useEditorStore.filePath` (the *open* file) — `cursor` and `active` are the two independent selection states `FileTreeItem` renders (doc/v0-spec.md §5.3). |
| FileTreeItem | `FileTreeItem` (`src/components/vault/FileTreeItem.tsx`) | "file node row", "tree row" | Hand-built sidebar row (design system `vault` group, doc/v0-spec.md §5.6): `kind`/`depth`/`active`/`cursor`/`collapsed` props. `active` = the file open in `useEditorStore` (raised `--surface-2`); `cursor` = `useSidebarStore.cursorPath` (inset `--border-active` bar, no fill). Its glyphs live in `src/components/vault/glyphs.tsx`, shared with `EntryDraftRow` and the vault header's create buttons. |
| Entry draft | `EntryDraft` / `EntryDraftKind` (`src/stores/sidebar-store.ts`) | "pending file", "new file state", "rename state" (there is no rename in v0) | A vault entry being named but not yet on disk (#40): a `kind` (`'note' \| 'directory'`, mirroring the two creation commands) and the `parentPath` it will land in — always an already-existing directory, since `create_*` never materializes a parent chain. At most one exists at a time. `beginDraft` also expands `parentPath`, so the draft row is on screen even when the target directory was folded. |
| EntryDraftRow | `EntryDraftRow` (`src/components/vault/EntryDraftRow.tsx`) | "new file input", "inline rename" | The sidebar row that renders an `EntryDraft`: `FileTreeItem`'s geometry with an auto-focused `<input>` in the label slot, placed at the position the entry will occupy so the target directory reads off the indent alone. `Enter` commits via `createVaultEntry`, `Escape` and blur cancel. Its input is why `useSidebarKeymap` goes inert while a draft is open — that hook guards on `isEditableTarget`. |
| Create vault entry (helper) | `createVaultEntry` (`src/lib/vault/create-entry.ts`) | "new note action", "touch helper" | Shared by the vault header's two buttons and `useSidebarKeymap`'s `a`/`A` (the `openVaultFile` pattern): validates the typed name — a *name*, never a path, so a `/` or `\` is rejected — calls `createNote`/`createDirectory`, then cancels the draft, moves `cursorPath` onto the created path, and opens the note. Never touches `useSidebarStore.tree`; the create trips the `notify` watcher and `Sidebar`'s existing refetch does the rest. A failed create (usually `AlreadyExists`) surfaces as an error toast and **keeps** the draft up so the name can be corrected. The same file owns the two pure helpers `resolveDraftParent` (where a new entry belongs, given the cursor) and `draftPlacement` (where its row renders). |
| Sidebar keymap | `useSidebarKeymap` (`src/hooks/use-sidebar-keymap.ts`) | "tree keymap", "j/k handler" | Vim-style `j`/`k`/`l`/`h`/`Enter` navigation over the sidebar's flattened visible rows, plus `a` (new note) / `A` (new folder) starting an `EntryDraft` (#40). Scoped like `useGlobalKeymap`: only acts while `activeRegion === 'sidebar'` and `mode === 'normal'`, so it never steals keystrokes from the editor or the command line. `a`/`A` are handled *before* the empty-tree bail-out — an empty vault is exactly when the first note has to be creatable. |
| Save loop | `useSaveLoop` (`src/hooks/use-save-loop.ts`) | "autosave", "persist hook" | Mounted once in `App.tsx`. Watches `useEditorStore.saveIntent`; on every bump, writes `content` to `filePath` via `vaultService.writeNote` and calls `markSaved`. No-ops for the scratch buffer (`filePath === null`) — v0 has no "save as" flow. |
| Open file (helper) | `openVaultFile` (`src/lib/vault/open-file.ts`) | "load file", "select file" | Shared by `Sidebar`'s click handler and `useSidebarKeymap`'s `l`/`Enter`: reads the note via `vaultService.readNote`, loads it into `useEditorStore` via `openFile`, and moves `activeRegion` to `'viewer'`. |
| Claude Code backend | `createClaudeCodeBackend` (`src/lib/agent/backends/claude-code.backend.ts`) | "Claude adapter" (the term "adapter" is reserved for the general concept — see Flagged ambiguities) | The `AgentBackend` implementation for `id: 'claude-code'` (doc/v0-spec.md §4.4). Spawns `claude -p --input-format stream-json --output-format stream-json --verbose --allowedTools ...` via `agentProcessService` and translates its native stream into `AgentEvent`s. Field names are pinned against a real captured stream (Claude Code 2.1.204), not just the CLI's `--help` text. |
| Codex backend | `createCodexBackend` (`src/lib/agent/backends/codex.backend.ts`) | "Codex adapter" (the term "adapter" is reserved for the general concept — see Flagged ambiguities) | The second `AgentBackend` implementation, for `id: 'codex'` (doc/v0-spec.md §4.4). Unlike Claude Code's single long-lived process, `codex exec --json` runs one turn to completion and exits ("resumable sessions"): `start` only records `cwd`/`onEvent`, and each `send` spawns a fresh `codex exec [resume <thread_id>] --json --full-auto <prompt>` process, replacing the prior turn's exited one, with `keepStdinOpen: false` (#51) so the process doesn't hang reading for stdin input that will never arrive. `CodexItem`'s discriminant field is `type` (not `item_type`) and `turn.failed`'s message is nested at `error.message` (not top-level `message`) — both re-pinned (#51) against a captured real stream from Codex CLI 0.143.0, now installed locally, superseding the earlier best-effort mapping written before Codex was available (epic Open Questions, `.agents/plans/2026-07-05-epic-orbit-111-v0.md`). |
| agentProcessService | `agentProcessService` (`src/services/agent-process.service.ts`) | "process service" | Typed wrapper over the generic Rust `agent_spawn`/`agent_send`/`agent_stop` commands and the `agent-stdout`/`agent-stderr`/`agent-exit` events. Engine-agnostic: forwards raw stdout/stderr lines verbatim and never parses them — parsing a line into an `AgentEvent` is each `AgentBackend`'s own adapter's job. `spawn`'s `keepStdinOpen` (default `true`, #51) tells `agent_spawn` whether to leave the child's stdin writable for later `send` calls (Claude Code's long-lived process) or close it right after spawn (Codex's one-shot `codex exec`, which otherwise blocks reading stdin for more input it will never receive). |
| AgentProcessState | `AgentProcessState` (`src-tauri/src/agent.rs`) | — | Rust-side `tauri::State`: the single running agent child process (if any) plus its stdin handle — `Option<ChildStdin>` (#51), `None` once `agent_spawn`'s `keep_stdin_open: false` closes the pipe immediately after spawn. `agent_send` returns `StdinClosed` if called while `stdin` is `None`. `agent_spawn` replaces (kills) any process already running before starting the new one — mirrors `VaultState`'s watcher-replacement pattern. `agent_spawn` resolves the bare `command` to an absolute executable against an augmented PATH (inherited + common macOS user bin dirs: Homebrew, `/usr/local/bin`, `~/.local/bin`, `~/.cargo/bin`, etc.) before spawning, and passes that same augmented PATH to the child's env — so a correctly-installed engine is found even when the app is launched outside a login shell (packaged app / Finder / Spotlight). A command that can't be resolved (missing, or a dangling symlink) returns `CommandNotFound { command }` rather than the OS's raw "No such file or directory". |
| Chat store | `useChatStore` (`src/stores/chat-store.ts`) | "chat state" | Zustand store owned by the chat slice. Holds the transcript as `ChatItem[]` — merging the user's own locally-recorded turns with the selected `AgentBackend`'s `AgentEvent` stream — plus `turnActive` (disables `ChatInput`) and `sessionStarted`. Also holds `modelId` (an `AgentModelId`); it derives the engine via `getModel(modelId).engine`, looks the backend up with that, and passes the model's `cliModel` to `start`. `setModel` (was `setBackend`) restarts the session on any model change. Starts the backend lazily on the first `sendMessage`, using `useAppStore.vaultRoot` as its `cwd`; errors if no vault is open rather than depending on the sidebar/vault-open slice (#14). |
| ChatItem | `ChatItem` (`src/stores/chat-store.ts`) | "message" | Discriminated union rendered by `ChatPanel`: `user_message` (local, never an `AgentEvent`), `agent_message` / `agent_reasoning` / `tool_call` / `error` (derived 1:1 or accumulated from `AgentEvent`s by `useChatStore`). |
| ChatMessage | `ChatMessage` (`src/components/ai/ChatMessage.tsx`) | "message bubble" | Renders one chat turn; `role: 'user' \| 'agent'` (not a DOM ARIA role — see Flagged ambiguities), `streaming` reserved for the future delta-streaming evolution (doc/v0-spec.md §4.3). Renders only `agent_message`/`user_message` — reasoning has its own `ReasoningBlock` (#49). |
| ReasoningBlock | `ReasoningBlock` (`src/components/ai/ReasoningBlock.tsx`) | "thinking bubble" | Renders an `agent_reasoning` `ChatItem` (#49): no border, dimmer (`text-dim`) and italic, so it reads as subordinate to `ChatMessage`'s final-answer bubble without introducing color (accent discipline, doc/v0-spec.md §5.6). |
| ThinkingIndicator | `ThinkingIndicator` (`src/components/ai/ThinkingIndicator.tsx`) | "spinner", "loading dots" | The in-flight loader (#49): rendered by `ChatPanel` for the full duration of `useChatStore.turnActive`, appended after the transcript so the agent's activity stays visible between events, not just before the first one. |
| ToolChip | `ToolChip` (`src/components/ai/ToolChip.tsx`) | "tool badge" | Renders a `tool_call_start` (+ its eventual `tool_call_result`) as a chip. `status: 'running' \| 'done' \| 'error'` — the `error` status is the extension to the reference component flagged in doc/v0-spec.md §4.3/§8. |
| ChatInput | `ChatInput` (`src/components/ai/ChatInput.tsx`) | "prompt input" | The chat's `›`-prompt row (doc/v0-spec.md §5.2); disabled while `useChatStore.turnActive`. |
| Settings | `Settings` (`src-tauri/src/settings.rs`), mirrored as a TS interface in `src/services/settings.service.ts` | "preferences", "config" (as type names) | The persisted user settings (epic #24): `model` (an `AgentModelId`, whose engine is derived from the catalog), `editorFont`, `theme` (a `ThemeName`: `'dark' \| 'light'`), `vaultPath`. Stored as `settings.json` in the platform app-config dir; every field has a serde default so older/hand-edited files still parse. A pre-`model` file's stale `engine` key is ignored and `model` falls back to `gpt-5.4-mini`. `vaultPath` defaults to `~/Documents/orbit-brain` (resolved by `read_settings`). Theme/font are applied by `useApplyTheme` (#28); the dialog UI is `SettingsDialog` (#29). |
| settingsService | `settingsService` (`src/services/settings.service.ts`) | "settings API" | Typed wrapper over the Rust `read_settings`/`write_settings` commands. Only `useSettingsStore` calls it — components and other stores go through the store. |
| Settings store | `useSettingsStore` (`src/stores/settings-store.ts`) | "preferences store" | Zustand store owned by the settings slice (#25): the in-memory mirror of the settings file. `loadSettings()` reads from disk once (idempotent); `updateSettings(patch)` merges, sets optimistically, and writes through via `settingsService.writeSettings`. |
| Settings bootstrap | `useSettingsBootstrap` (`src/hooks/use-settings-bootstrap.ts`) | "startup hook", "init hook" | Mounted once in `App.tsx`. On boot: loads settings, seeds `useChatStore`'s model (only if the catalog knows it and no session started), then opens the vault at `settings.vaultPath` via `open_vault` — creating the default vault on first run — and seeds `useAppStore.vaultRoot`. On failure the app degrades to the manual "Open vault…" flow. |
| Default vault | `~/Documents/orbit-brain` (resolved in `src-tauri/src/settings.rs`) | — | The vault `open_vault` creates and adopts on first boot, so the app starts usable without a picker. `open_vault` (in `vault.rs`) is `pick_vault` minus the dialog: `create_dir_all` + watcher + canonical path back to the frontend. |
| Backend logger | `logging::plugin` / `LogResult` (`src-tauri/src/logging.rs`) | "log utility", "tracing" | The backend logging utility (used by every Tauri command). Configures `tauri-plugin-log` to append leveled, timestamped lines to `logs/orbit-111.log` (relative to the app CWD — `src-tauri/` under `tauri dev`) + stdout. `LogResult::log_err(command)` is the extension every command chains onto its result to record failures. Not to be confused with the child-process stderr that `agent.rs` forwards to the frontend — that is engine output, not backend logs. |
| Toast | `Toast` / `useToastStore` (`src/stores/toast-store.ts`) | "notification", "snackbar", "alert" | A transient, auto-dismissing feedback line with `tone: 'info' \| 'error'` (#27). Monochrome per the accent discipline (doc/v0-spec.md §5.6) — tones differ by border weight + a tracked-caps tag, never color. Info toasts fade after 2s, error toasts after 6s; fades only (motion tokens). `showToast` is the single entry point; v0's only producer is `useSaveLoop`. |
| ToastHost | `ToastHost` (`src/components/layout/ToastHost.tsx`) | "toaster", "toast container" | Bottom-center overlay rendering the toast stack, mounted once in `Layout` alongside `CommandBar`/`AiFab`. Sole consumer of `useToastStore`. Click a toast to dismiss it early; `aria-live="polite"` for screen readers. |
| Settings dialog | `SettingsDialog` (`src/components/layout/SettingsDialog.tsx`) | "preferences modal", "settings panel" | Modal overlay (#29) editing the four persisted settings through `useSettingsStore.updateSettings`. Selecting a model also calls `useChatStore.setModel` (which switches the derived engine) immediately — the reactive wiring #25 deferred; theme/font values are applied live by `useApplyTheme` (#28). Visibility is `useAppStore.settingsOpen` (an overlay flag, not a `FocusRegion`); opened from the StatusBar `settings` button or `Ctrl-w` `s`, closed by Escape or a scrim click. Keystrokes inside the panel stop propagation so the global keymap never fires under the modal. |
| Pick vault (helper) | `pickAndPersistVault` (`src/lib/vault/pick-vault.ts`) | "vault picker" | Shared by the Sidebar's "Open vault…" action and `SettingsDialog`'s vault row (the `openVaultFile` pattern): native folder picker → `useAppStore.setVaultRoot` → persist `settings.vaultPath`. Resolves to `null` when the picker is cancelled. |
| Theme application | `useApplyTheme` (`src/hooks/use-apply-theme.ts`) | "theme switcher", "theme hook" | Mounted once in `App.tsx` (#28). Applies `settings.theme` as the `data-theme` attribute on `<html>` — the light palette in `src/styles/tokens/colors.css` keys off `:root[data-theme='light']`, dark stays the attribute-less default — and `settings.editorFont` as the `--editor-font` variable (quoted family + `--font-mono` fallback stack). Reactive to `useSettingsStore`, so `SettingsDialog`'s (#29) changes apply live. |
| Editor font (token) | `--editor-font` (`src/styles/tokens/typography.css`) | "app font" (it is editor-only) | The editor's font-family slot, defaulting to `var(--font-mono)` and overridden inline by `useApplyTheme`. Read only by the CM6 `editorTheme` — the app chrome deliberately stays on `--font-mono` (one-face rule, doc/v0-spec.md §5.6). |

---

## Relationships

- `useAppStore.activeRegion` (a `FocusRegion`) is only ever set to `'chat'` while `chatOpen` is `true`, and to `'sidebar'` while `sidebarOpen` is `true`; closing either region reassigns `activeRegion` to `'viewer'` if that region was active. `'viewer'` is unconditionally reachable, so `reachableRegions` never returns an empty list.
- `StatusBar`'s `sidebar` button and the `Ctrl-w` `b` chord (`useGlobalKeymap` and `regionExit` alike) are the only entry points to `useAppStore.toggleSidebar`; `Sidebar` is the sole consumer of `sidebarOpen`, self-guarding with `return null` *after* its hooks (as `ChatPanel` does for `chatOpen`) so the `vault-changed` watcher keeps the tree fresh while hidden.
- `useGlobalKeymap` reads and writes `useAppStore` state exclusively — it holds no state of its own beyond the transient `Ctrl-w` prefix-arm timer.
- `AiFab` is the only UI entry point that calls `useAppStore.toggleChat`; the global keymap's `Ctrl-w c` chord calls the same action, and so does the editor's own `regionExit` chord.
- `Viewer` (`src/components/layout/Viewer.tsx`) is the sole consumer of `useEditorStore`; `regionExit` reads/writes `useAppStore` the same way `useGlobalKeymap` does, but from inside the CM6 extension rather than a `window` listener.
- `read_note`/`write_note` accept a `path` string that must resolve (after canonicalization) inside the current `VaultState` root — in practice this should always be a `path` value previously returned by `read_vault_tree`, since arbitrary strings are rejected by the guard.
- `vaultService.pickVault()` resolves to the chosen folder's path; it does not itself call `useAppStore.setVaultRoot` — that wiring belongs to whichever slice invokes it (`Sidebar`'s "Open vault…" action, #14).
- `create_note`/`create_directory` never touch `useSidebarStore.tree`: creating an entry trips the `notify` watcher, which emits `vault-changed`, which drives `Sidebar`'s existing refetch — so a new note or folder appears through the same watcher path as an externally-created one, with no manual tree invalidation.
- `scaffold_agent_context` runs in both vault-adopt paths (`open_vault` and `pick_vault`), and deliberately *before* `watch` — so the frontend's first `read_vault_tree` already sees the pair and the app's own boot-time writes never emit a `vault-changed`. It is best-effort: a root that cannot be written to (read-only mount, missing permissions) logs a warning and still opens, leaving the agent to run without project instructions.
- Because `useSettingsBootstrap` calls `open_vault` on every boot, `scaffold_agent_context` runs on every boot too. It is idempotent by construction (`create_new` per file), so the user's edits survive and only a deleted file is restored.
- No `AgentBackend` reads the agent-context pair: both engines pick it up themselves from their `cwd` (`useChatStore` passes `useAppStore.vaultRoot` to `AgentBackend.start`). Neither backend may pass a flag that skips project instructions (e.g. Claude Code's `--bare`, doc/v0-spec.md §4.4).
- Because `build_tree` keeps empty directories (#36), a directory created by `create_directory` is visible in the sidebar immediately; it renders as a `FileTreeItem` with `kind="dir"` whose `children` is `[]`, which `flattenVisibleTree` already handles (expanding it simply yields no rows).
- `build_tree` filters `AGENT_CONTEXT_FILES` names only at the vault root (#50) — a nested note that happens to be named `AGENTS.md` (e.g. inside a subdirectory) still renders normally, since only the two scaffolded root files are meant to stay off the sidebar.
- `AgentBackend` implementations are looked up by `AgentSource` through the backend registry (`src/lib/agent/backend-registry.ts`); the chat panel never imports a concrete backend directly — only `AgentEvent` and the registry functions.
- `Sidebar` (`src/components/layout/Sidebar.tsx`) is the sole consumer of `useSidebarStore`; it refetches `vaultService.readVaultTree()` whenever `useAppStore.vaultRoot` changes and whenever `vaultService.onVaultChanged` fires, so the tree stays in sync with the watcher without surgical diffing.
- `openVaultFile` and `useSidebarKeymap` both write `useSidebarStore.cursorPath` and `useEditorStore` (via `openFile`) but never `useSidebarStore.tree` — the tree is owned exclusively by `Sidebar`'s watcher-driven refresh.
- The vault header's two buttons and `useSidebarKeymap`'s `a`/`A` are the only entry points to `useSidebarStore.beginDraft`; both resolve the target directory through the same `resolveDraftParent`, so mouse and keyboard can never disagree about where a new entry lands. `Sidebar` is the sole consumer of `draft`, splicing one `EntryDraftRow` into its rendered rows at `draftPlacement`.
- `createVaultEntry` is the only caller of `vaultService.createNote`/`createDirectory`, and it reaches `useEditorStore` only through `openVaultFile` — so "a new note is opened exactly like an existing one" holds at the module boundary, not just by convention.
- `useSaveLoop` reads `useEditorStore.filePath`/`content` and calls `vaultService.writeNote` directly; it does not go through `Sidebar` or `useSidebarStore`, so saving is independent of the sidebar's own render/mount state.
- `useSaveLoop` is v0's only `useToastStore.showToast` producer: `Saved <file>` (info) on `writeNote` success, `Save failed: <reason>` (error) on rejection — `markSaved` only runs on success, so a failed save keeps the buffer `dirty`.
- `useChatStore.sendMessage` is the only caller of `AgentBackend.start`/`send` outside the registry itself; it resolves the model via `getModel(modelId)`, looks the backend up via `getBackend(model.engine)`, passes `model.cliModel` to `start`, and folds every `AgentEvent` the backend emits into `items` via `applyAgentEvent`.
- `createClaudeCodeBackend` is the only consumer of `agentProcessService` — the chat store and `ChatPanel` never call it directly, keeping "only `AgentEvent` leaves the adapter" true at the module boundary, not just by convention.
- `ChatPanel` no longer renders a backend selector (removed 2026-07-10): the model is chosen only in `SettingsDialog`, which calls `useChatStore.setModel` — stopping the previously-running backend if a session had started — alongside persisting `settings.model`. The engine is derived from the selected model.
- `useSettingsStore` is the sole caller of `settingsService`; `useSettingsBootstrap` is the sole boot-time writer of `useAppStore.vaultRoot` and never overwrites a vault the user already opened.
- `Sidebar`'s "Open vault…" action and `SettingsDialog`'s vault row both go through `pickAndPersistVault`, which adopts the picked folder as `vaultRoot` and persists it into `settings.vaultPath` so a manual pick survives restarts; `useSettingsBootstrap` reads it back on the next boot.
- `settings.model` *seeds* `useChatStore.modelId` at boot via `setModel` (#29), and `SettingsDialog` writes it and applies it immediately via `setModel`. With the `ChatPanel` selector removed, `SettingsDialog` is the sole way to switch models — and thus engines, derived from them — so every model change is now persisted (the per-session override path is gone).
- `StatusBar`'s `settings` button and the `Ctrl-w` `s` chord (`useGlobalKeymap` and `regionExit` alike) are the only entry points to `useAppStore.openSettings`/`toggleSettings`; `SettingsDialog` is the sole consumer of `settingsOpen`.
- `useApplyTheme` reads `useSettingsStore` reactively and touches only `document.documentElement` (the `data-theme` attribute + the `--editor-font` inline variable); it never writes settings. `Viewer` separately selects `settings.theme` to rebuild `editorTheme(dark)` (CM6's `dark` base-theme flag), memoized so the extension array identity only changes on an actual theme switch.

---

## Flagged ambiguities

- **"Vim mode" is two distinct systems** (doc/v0-spec.md §3.4): the app-level `GlobalMode` (`normal`/`command`, app shell slice) and the editor's own CodeMirror vim mode (`EditorVimMode`, editor slice, backed by `@replit/codemirror-vim`). Do not merge these into one enum or one store — they are deliberately separate state machines that only hand off focus at the region boundary.
- **"Adapter" vs `AgentBackend`** (doc/v0-spec.md §4.3): the spec's "adapter" is the per-engine translator (`createClaudeCodeBackend` built in #15, `createCodexBackend` built in #16) that implements the `AgentBackend` interface and turns a native stream into `AgentEvent`s. `AgentBackend` is the interface itself, not an implementation. Don't use "adapter" to refer to the interface.
- **"Collapsed" is two distinct things in the sidebar.** `sidebarOpen === false` hides the **whole sidebar region** (#38, app shell slice, `useAppStore`). `FileTreeItem.collapsed` describes **one directory row** whose children are folded away (`useSidebarStore.expanded`, sidebar slice). Don't call a hidden sidebar "collapsed", and don't call a folded directory "hidden".
- **"Draft" (sidebar) is not "dirty" (editor).** An `EntryDraft` is a vault entry that does not exist on disk *at all* yet — a row being named (#40). A `dirty` buffer is a note that *does* exist and has unsaved edits (`useEditorStore`). Nothing bridges them: committing a draft creates an **empty** file and opens it clean.
- **`ChatMessage`'s `role` prop is not a DOM ARIA role.** It's the design system's own `'user' | 'agent'` prop (doc/v0-spec.md §5.6 component inventory), coincidentally named the same as the HTML/ARIA `role` attribute. Biome's `lint/a11y/useValidAriaRole` flags it as a false positive at call sites — silenced there with a narrow `biome-ignore`, not a rename, to keep the prop name matching the spec's component contract.
- **Engine vs Model.** The *engine* (`AgentSource`: `claude-code`/`codex`) is which CLI is spawned; the *model* (`AgentModel`) is which LLM that CLI runs, via `--model`. The user picks a model; the engine is derived from it and is no longer a standalone setting. Don't persist an engine separately from the model.

---

## Changelog

| Date | Change | Reason |
|------|--------|--------|
| [YYYY-MM-DD] | Initial scaffold | Project created |
| 2026-07-08 | Added `FocusRegion`, `GlobalMode`, `useAppStore`, `useGlobalKeymap`, `AiFab` | App shell slice (#10): focus/mode state machine + global vim keymap foundation |
| 2026-07-08 | Added `useEditorStore`, `EditorVimMode`, `saveIntent`, `regionExit`, `wikilink` | Editor slice (#11): CodeMirror 6 markdown editor with vim + `Ctrl-w` coexistence |
| 2026-07-08 | Added `VaultEntry`, `VaultState`, `vaultService`, `vault-changed` | Filesystem slice (#12): Rust vault read/write commands + `notify` watcher |
| 2026-07-08 | Added `AgentEvent`, `AgentSource`, `AgentBackend`, backend registry | Agent event contract slice (#13): pure `AgentEvent` discriminated union + `AgentBackend` interface + backend registry so the chat can be built (#15) against a stable contract |
| 2026-07-08 | Added `useSidebarStore`, `FileTreeItem`, `useSidebarKeymap`, `useSaveLoop`, `openVaultFile`; extended `useEditorStore` with `filePath`/`openFile` | Sidebar open/save loop (#14): collapsible vault explorer wired to open/edit/save `.md` files, watcher-driven refresh — completes Half A |
| 2026-07-08 | Added `AgentProcessState`, `agentProcessService`, `createClaudeCodeBackend`, `useChatStore`, `ChatItem`, `ChatMessage`, `ToolChip`, `ChatInput` | Chat + Claude backend slice (#15): generic Rust process-spawn, the Claude Code adapter (translation pinned against a captured real stream), the chat store, and the `ai/*` UI that renders the `AgentEvent` stream — completes Half B |
| 2026-07-08 | Added `createCodexBackend` | Codex backend adapter slice (#16): second `AgentBackend`, registered via `registerBuiltinBackends()` with zero UI changes — proves adapters are the growth point. Mapping is best-effort (Codex not installed locally), pending re-pin against a captured stream |
| 2026-07-08 | Added `logging::plugin` + `LogResult` | Backend file logger: `tauri-plugin-log` writing `logs/orbit-111.log`, entry+error logs wired into all seven Tauri commands |
| 2026-07-09 | `agent_spawn` command resolution + `CommandNotFound`; chat surfaces spawn failures | Fix Codex `os error 2` (broken install) UX and make GUI-launched apps find installed engines |
| 2026-07-09 | Added `Settings`, `settingsService`, `useSettingsStore`, `useSettingsBootstrap`, `open_vault`, default vault | Settings foundation slice (#25, epic #24): persisted `settings.json` + default vault bootstrap (`~/Documents/orbit-brain`) so #28/#29 build on a stable contract |
| 2026-07-09 | Added `Toast`/`useToastStore`, `ToastHost`; `useSaveLoop` surfaces save success/failure | Toast notifications for save feedback (#27): `:w`/`Mod-s` now confirm the write or report the error |
| 2026-07-09 | Added `useApplyTheme`, `--editor-font`, light token palette (`:root[data-theme='light']`); `editorTheme` became a `(dark)` factory | Apply theme & editor font settings (#28, epic #24): `settings.theme`/`settings.editorFont` now take effect (live), completing the "stored but not applied" hand-off from #25 |
| 2026-07-09 | Added `SettingsDialog`, `pickAndPersistVault`; `settingsOpen` trio on `useAppStore`; `Ctrl-w s` chord in both keymaps | Settings dialog (#29, epic #24): edit engine/theme/font/vault from the app; engine changes apply to the chat reactively |
| 2026-07-10 | Added `create_note`/`create_directory` (+ `vaultService.createNote`/`createDirectory`, `VaultError::AlreadyExists`); `build_tree` now keeps empty directories | Vault entry creation backend (#36): the guarded creation commands a later sidebar "new note / new folder" UI slice builds on, plus the tree change that makes a freshly created empty folder visible |
| 2026-07-10 | Added `sidebarOpen` trio on `useAppStore`; generalized `reachableRegions`; `Ctrl-w b` chord in both keymaps; StatusBar `sidebar` button | Hideable sidebar (#38): the sidebar becomes the second gated `FocusRegion`, mirroring `chatOpen` so the focus state machine stays total |
| 2026-07-10 | `AiFab` re-anchored bottom-right → top-right; `ChatPanel`'s header bar (rule + "Chat" title) removed, engine selector kept bare and left-aligned | Chat chrome (#39): the FAB no longer collides with `Viewer`'s vim-mode badge, and the chat opens straight into the transcript |
| 2026-07-10 | Added `EntryDraft`/`EntryDraftKind` + `beginDraft`/`cancelDraft` on `useSidebarStore`, `EntryDraftRow`, `createVaultEntry` (with `resolveDraftParent`/`draftPlacement`), `a`/`A` sidebar chords, vault-header create buttons; `FileTreeItem`'s glyphs extracted to `src/components/vault/glyphs.tsx` | Sidebar new-file / new-folder affordance (#40): the UI slice that consumes #36's guarded creation commands, so a vault can be filled from inside the app |
| 2026-07-10 | Added `AGENT_CONTEXT_FILES`/`scaffold_agent_context` + `src-tauri/templates/vault-{agents,claude}.md`; `open_vault`/`pick_vault` scaffold before watching | Vault agent context (#41): every adopted vault root carries the `AGENTS.md`/`CLAUDE.md` pair the headless engines load from their `cwd` (doc/v0-spec.md §4.4), so the agent knows it is writing wikilinked notes rather than co-editing the open file |
| 2026-07-10 | Removed `ChatPanel`'s engine-selector row (`listBackends()`/`Badge` buttons), replaced with an empty spacer reserving the FAB band | Engine is now chosen only in `SettingsDialog`; drops the redundant, non-persisted per-session override |
| 2026-07-10 | Added `AgentModel`/`AgentModelId` + model catalog (`src/lib/agent/model-catalog.ts`); `Settings.engine`→`Settings.model`; `AgentBackend.start` gains a `model` arg both backends pass to the CLI `--model` flag; `useChatStore.backendId`/`setBackend`→`modelId`/`setModel` (engine derived); SettingsDialog Engine row → Model row | Settings model selector: the user picks an LLM (GPT 5.4 mini / Haiku 4.5 / Sonnet 5 / GPT 5.5 fast); the engine follows from the model |
| 2026-07-10 | Added `AgentReasoningEvent`/`agent_reasoning` to `AgentEvent`/`ChatItem`; both backends now emit `thinking`/`reasoning` blocks as `agent_reasoning` instead of folding them into `agent_message`; added `ReasoningBlock` and `ThinkingIndicator` (`src/components/ai/`); `ChatPanel` renders the loader for the full `turnActive` duration | Chat thinking UX (#49, epic #48): the agent's reasoning is now a distinct, visually subordinate transcript item, and an in-flight loader keeps the agent's activity visible between events, not just before the first one |
| 2026-07-10 | `build_tree` (`src-tauri/src/vault.rs`) now skips `AGENT_CONTEXT_FILES` names (`AGENTS.md`/`CLAUDE.md`) at the vault root | Hide agent context files from the sidebar tree (#50): they're project instructions for the headless engines, not user-facing notes — still directly readable/writable, just not sidebar-navigable |
| 2026-07-10 | `CodexItem`'s `item_type`→`type` field rename and `turn.failed`→`error.message` fix in `codex.backend.ts`; `agent_spawn`/`agentProcessService.spawn` gained `keepStdinOpen` (`RunningAgent.stdin` now `Option<ChildStdin>`, new `StdinClosed` error); Codex backend spawns with `keepStdinOpen: false` | Fix Codex backend (#51): re-pinned the event mapping against a captured real stream now that Codex CLI is installed locally (the earlier best-effort mapping never matched, so no Codex event ever reached the UI), and fixed every Codex turn hanging forever because `codex exec` blocks reading a piped stdin left open for more input |
